# خطة تثبيت مشروع AI Opportunity Agent

الهدف: مشروع دورة بسيط يمكن شرحه، مبني على Flask وSQLite وSocket.IO، وعلى أفكار Homework 1 و2. لا تغيير للواجهة في هذه المرحلة.

1. **Prompts:** قالب Jinja موحّد يستخدم role/domain/instructions/context/examples، مع بقاء كل Expert في ملفه. CSV مصدر إعدادات الأدوار ويُحدّث الجدول دون حذف بيانات المستخدم.
2. **Workflow:** حفظ الحالة وآخر حدث في SQLite؛ إعادة عرض الحالة عند الاتصال؛ تمييز المهمة المنقطعة بعد إعادة التشغيل. التشغيل المحلي بعملية واحدة، دون استئناف تلقائي أو queue خارجية.
3. **Semantic Search:** التحقق من المتجهات واسم النموذج وأبعادها، إصلاح cache غير المتوافق، وتوفير نتيجة صادقة عند تعذر البحث الدلالي. التشابه ليس احتمال قبول في وظيفة.
4. **اعتمادية backend:** retries محدودة للأخطاء المؤقتة فقط، تحقق من المدخلات، API يعرض حقولًا محددة، تفعيل JOBS_LIMIT، وسجلات مراحل لا تحتوي نصوص السير الذاتية.
5. **Chat:** آخر ست رسائل كسياق للـOrchestrator والخبير، حفظ اختيار الوظيفة في سجل المحادثة، وتنفيذ الأفعال بدوال Python محددة وJSON صالح.
6. **Verification:** اختبارات unittest على قاعدة مؤقتة وطلبات خارجية محاكية؛ اختبار الصلاحيات والفشل الجزئي والحالة والـprompts والذاكرة.
7. **Documentation:** شرح المسار، التشغيل، الاختبارات، وحدود المشروع. تطوير التصميم وميزة CV Improvement Expert مرحلتان لاحقتان.

معيار الإنجاز: نجاح الاختبارات دون استخدام بيانات المستخدم أو API مدفوع، والمحافظة على عقود الواجهة الحالية. ملفات PDF التاريخية يمكن نقلها لأرشيف محلي قابل للاسترجاع؛ لا حذف تلقائي لبيانات المستخدم.

## نتيجة التنفيذ

اكتملت المراحل 1–7. نجح 25 اختبارًا، بينها تشغيل `run.py` على قاعدة مؤقتة وفحص الصفحة وSocket.IO عبر HTTP محلي. ملفات الواجهة لم تُعدّل. يبدأ تحديث schema والأدوار لقاعدة المشروع عند التشغيل التالي؛ الاختبارات استخدمت قواعد مؤقتة فقط.

تم نقل ملفات PDF التاريخية العشرة إلى `private_archive/legacy_uploads` بدل حذفها. المجلد مستثنى من Git، ويمكن استرجاع الملفات منه؛ هذا نقل محلي وليس محوًا للبيانات الحساسة.

المؤجل: تصميم الواجهة، عرض التشابه والتنبيه عند degraded، وCV Improvement Expert الاختياري. لا يوجد استئناف تلقائي للعمل المنقطع ولا اختبار جودة حي عبر OpenRouter في هذه المرحلة.
